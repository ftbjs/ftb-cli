const {
  {{#if eslint}}
  webpackEsLintConfig,
  {{/if}}
  resolve,
  isProd
} = require('./utils')

module.exports = {
  entry: {
    main: isProd ? resolve('../src/index.js') : resolve('../src/index.dev.js')
  },
  module: {
    rules: [
      {{#if eslint}}
      webpackEsLintConfig(),
      {{/if}}
      {
        test: /\.js$/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env']
          }
        }
      }
    ]
  }
}
